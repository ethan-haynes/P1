(function($) {
    'use strict';
    //scope safe

    //Document ready
    $(function() {
        //focus on page load
        $('.landing').focus();

        $( window ).scroll(function() {
            //ease to main page
            //make navbar fixed at top
        });
    });
})(jQuery);
